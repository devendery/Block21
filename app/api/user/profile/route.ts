import { NextRequest, NextResponse } from "next/server";

// Placeholder for user profile API
// Previous game-specific implementation has been removed.

export async function GET(req: NextRequest) {
  return NextResponse.json({ message: "Profile API under reconstruction" });
}

export async function POST(req: NextRequest) {
  return NextResponse.json({ message: "Profile API under reconstruction" });
}
